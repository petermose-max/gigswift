# Development & CI dependencies. Installs production deps too, so a single
#   pip install -r requirements-dev.txt
# gives a complete local environment.
-r requirements.txt

# Linting / formatting (replaces flake8 + black + isort)
ruff>=0.5,<1.0

# Testing
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<0.24
pytest-cov>=5.0,<6.0
httpx>=0.27,<0.29
aiosqlite>=0.20,<0.21  # async SQLite driver for the in-memory test database
